file encryption tool using Typage with WebAuthn PRF. It will be:

Simpler to implement and maintain
More reliable (battle-tested WebAuthn integration)
Interoperable with CLI tools
Just as secure for the browser threat model

Would you like me to create this artifact? I can build:

File upload and encryption UI with drag-and-drop
Passkey-based encryption (discoverable credentials)
Security key support (non-discoverable credentials)
ASCII armor encoding for sharing
Complete decrypt flow with the same passkey

The user experience would be:

Create a passkey (one-time setup)
Upload file → encrypt with biometric/PIN → download encrypted file
Share encrypted file (via any channel)
Recipient uploads → decrypt with their passkey → access file